<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Beginner'建築</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="site-header sticky-top py-1 bg-black">
      <div class="container d-flex flex-column flex-md-row justify-content-between">
        <%= link_to("TOP", "/",class: "py-2 d-none d-md-inline-block") %>
        <%= link_to("サイト説明", "/home/description",class: "py-2 d-none d-md-inline-block") %>
        <%= link_to("求人情報", "/posts/jp",class: "py-2 d-none d-md-inline-block") %>
        <%= link_to("建築語録", "/home/analects",class: "py-2 d-none d-md-inline-block") %>
        <%= link_to("掲示板", "/home/board",class: "py-2 d-none d-md-inline-block") %>
        <%= link_to("お知らせ", "/home/notice",class: "py-2 d-none d-md-inline-block") %>
        <% if session[:user_id] || session[:company_id]%>
        <%= link_to("ログアウト", "/home/logout", method: "post", class: "py-2 d-none d-md-inline-block") %>
        <% end %>
      </div>
    </nav>
    <% if flash[:notice] %>
      <div class="flash　col-md-12 text-center bg-warning">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
